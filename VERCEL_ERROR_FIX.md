# 🔧 إصلاح خطأ النشر في Vercel

## ❌ الخطأ الذي حدث:
```
Cannot find module '../../src/app/articles/[slug]/page.js'
```

## 🔍 السبب:
ملفات `.next-dev/` (مخرجات التطوير المؤقتة) كانت مرفوعة في Git وتحتوي على إشارات خاطئة.

---

## ✅ الحل (3 خطوات بسيطة):

### الخطوة 1: حذف `.next-dev` من Git
افتح Terminal وشغل هذا الأمر:
```bash
git rm -r --cached .next-dev/
```

### الخطوة 2: حفظ التغييرات
```bash
git add .gitignore
git commit -m "Fix: Remove .next-dev from git tracking"
```

### الخطوة 3: رفع التحديثات
```bash
git push origin main
```

---

## 🚀 ماذا سيحدث بعد ذلك؟

✅ Vercel سيبدأ النشر تلقائياً
✅ هذه المرة بدون ملفات `.next-dev` الخاطئة
✅ البناء سينجح بإذن الله

---

## 📊 تابع النشر:
https://vercel.com/dashboard

---

## ✅ تم الإصلاح:
- أضفت `.next-dev/` إلى `.gitignore` ✓
- الآن فقط نفذ الأوامر أعلاه

---

**ملاحظة**: `.next-dev/` هي ملفات مؤقتة من Turbopack ولا يجب رفعها أبداً في Git
